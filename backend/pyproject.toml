[project]
name = "song-learner-backend"
version = "0.1.0"
description = "Chord detection backend for Song Learner"
requires-python = ">=3.9"
dependencies = [
    "fastapi[standard]>=0.128.1",
    "librosa>=0.10.0",
    "numpy>=1.24.0",
    "soundfile>=0.12.1",
]
